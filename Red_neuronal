/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package red_neuronal;

import java.util.Scanner;
import red_neuronal.Matriz;
/**
 *
 * @author Jhonathan
 */
public class Red_Neuronal {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Scanner leer = new Scanner(System.in);
        
        Matriz matriz = new Matriz();
        
        int [][] matrizidentidad = new int[10][10];
        int [][] matriz1 = new int[10][10]; 
        int [] vectorMatriz1 = new int[100];
        int [][] multVectorTrans1 = new int[10][10];
        int [][] w1 = new int[10][10];
        
        int [][] matriz2 = new int[10][10];
        int [] vectormatriz2 = new int[4];
        int [][] multVectorTrans2 = new int[4][4];
        int [][] w2 = new int[4][4];
        
        int [][] wtotal = new int[10][10];
        
        int [] matrizDibujo = new int[100];
        
        int [] solucion = new int[100];
        
        int [][] matriz3 = new int[10][10];
        int [] vectorMatriz3 = new int[100];
        int [][] multVectorTrans3 = new int[10][10];
        int [][] w3 = new int[10][10];
        int [][] matriz4 = new int[10][10];
        int [][] matriz5 = new int[10][10];
        
        
        matriz.matrizidentidad(matrizidentidad);
        System.out.println("Ingrese los valores de la primera matriz");
        /*
        matriz.llenarmatriz(matriz1);
        matriz.llenarvectormatriz(matriz1, vectorMatriz1);
        matriz.multiplicacionTraspuestaVectores(vectorMatriz1, multVectorTrans1);           
        matriz.restamatriz(multVectorTrans1, matrizidentidad, w1);
        matriz.mostrarmatriz(w1);
        System.out.println("Ingrese los valores de la segunda matriz");
        matriz.llenarmatriz(matriz2);
        matriz.llenarvectormatriz(matriz2, vectormatriz2);
        matriz.multiplicacionTraspuestaVectores(vectormatriz2, multVectorTrans2);
        matriz.restamatriz(multVectorTrans2, matrizidentidad, w2);
        matriz.mostrarmatriz(w2);
        
        System.out.println("W TOTAL ES IGUAL A:");
        matriz.sumamatriz(w1, w2, wtotal);
        matriz.mostrarmatriz(wtotal);
        
        System.out.println("Ingrese la matriz que piensa dibujar");
        matriz.llenarvector(matrizDibujo);
        matriz.respuesta(matrizDibujo, wtotal, solucion);
        matriz.mostrarvector(solucion, 4);
        */
        
        matriz.llenarmatriz(matriz1);
        matriz.llenarvectormatriz(matriz1, vectorMatriz1);
        System.out.println("Ingrese los valores de la segunda matriz");
        //matriz.llenarmatriz(matriz3);
        //matriz.llenarvectormatriz(matriz3, vectorMatriz3);
        //operaciones
        matriz.multiplicacionTraspuestaVectores(vectorMatriz1, multVectorTrans1);
        //matriz.multiplicacionTraspuestaVectores(vectorMatriz3, multVectorTrans3);
        matriz.llenarw(multVectorTrans1, matrizidentidad, w1);
        //matriz.llenarw(multVectorTrans3, matrizidentidad, w3);
        //matriz.llenarwtotal(w1, w3, wtotal);
        //reconocimiento
        System.out.println("Ingrese los datos para el reconocimiento");
        matriz.llenarvector(matrizDibujo);
        matriz.respuesta(matrizDibujo, w1, solucion);
        matriz.mostrarvector(solucion, 100);
        
    }
    
}
